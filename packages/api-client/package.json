{
  "name": "@erp/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "Typed HTTP client shared by Web + Mobile (auth, tenant header, retry).",
  "dependencies": {
    "@erp/types": "workspace:*"
  }
}
